import type { ReactNode } from 'react';

type Props = {
  children?: ReactNode;
}

export function {{ pascalCase name }}({ children }: Props) {
  return (
    <>
      <h1>{{ name }}</h1>
      {children}
    </>
  );
};
